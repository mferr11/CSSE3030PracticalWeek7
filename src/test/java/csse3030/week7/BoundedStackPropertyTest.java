package csse3030.week7;

import net.jqwik.api.*;
import net.jqwik.api.constraints.*;
import static org.junit.jupiter.api.Assertions.*;

class BoundedStackPropertyTest {

    // TODO (practical Part 2): add two @Property methods here —
    //   1. an invariant property: size() never exceeds capacity(),
    //      even if you try to push more items than the stack can hold.
    //   2. a round-trip property: pushing an item and immediately
    //      popping it returns that same item.
}
